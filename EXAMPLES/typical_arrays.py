#!/usr/bin/env python

fruits = ['apple', 'cherry', 'orange', 'kiwi', 'banana', 'pear', 'fig']
name = "Eric Idle"
knight = 'King', 'Arthur', 'Britain'

print(fruits[0], fruits[3], len(fruits))  # <1>
print(name[0], name[2], len(name))  # <2>
print(knight[0], knight[1], len(knight))  # <3>
